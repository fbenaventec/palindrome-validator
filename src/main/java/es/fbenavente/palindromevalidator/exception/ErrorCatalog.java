package es.fbenavente.palindromevalidator.exception;

public class ErrorCatalog {
    public static final String ARGUMENTS_MISSING = "Missing arguments: method and text are mandatory, " +
            "expected method as first parameter on method could be SIMPLE or BASIC, and text as second parameter.";
    private ErrorCatalog() {

    }
}
